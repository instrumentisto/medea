language: rust
# cache: cargo

install:
  - bash _ci/protoc_install.sh
  - rustc -vV
  - cargo -vV

jobs:
  allow_failures:
    - rust: nightly
      stage: build

  include:
    - name: Clippy
      stage: check
      rust: stable
      before_script: rustup component add clippy
      script: make lint

    - name: rustfmt
      stage: check
      rust: nightly
      cache: false
      env: DONT_INSTALL_PROTOC=1
      before_script: rustup component add rustfmt
      script: make fmt check=yes

    - name: medea-jason (stable)
      stage: build
      rust: stable
      before_script:
        - rm -f /home/travis/.cargo/bin/wasm-pack
        - curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      script:
        - make release.npm crate=medea-jason publish=no

    - name: medea (stable, optimized)
      stage: build
      rust: stable
      script: make build.medea optimized=yes

    - name: medea (beta)
      stage: build
      rust: beta
      script: make build.medea

    - name: medea (nightly)
      stage: build
      rust: nightly
      script: make build.medea

    - name: unit (stable)
      stage: test
      rust: stable
      script: make test.unit crate=@all

    - name: e2e signalling (stable)
      stage: test
      rust: stable
      env: DONT_INSTALL_PROTOC=1
      script: make test.e2e.signalling

    - name: e2e (latest Firefox, stable Rust)
      stage: test
      rust: stable
      env: DONT_INSTALL_PROTOC=1
      script: make test.e2e.firefox

    - name: e2e (latest Chrome, stable Rust)
      stage: test
      rust: stable
      env: DONT_INSTALL_PROTOC=1
      script: make test.e2e.chrome

    - name: crates.io
      stage: release
      if: (tag IS present) AND (tag =~ ^medea[a-z-]*-[0-9]+\.[0-9]+\.[0-9]+)
      cache: false
      before_script:
        - export MEDEA_CRATE_NAME=$(echo $TRAVIS_TAG \
                        | sed -E "s/^(medea[a-z-]*)-[0-9]+\.[0-9]+\.[0-9]+/\1/")
      script:
        - echo "Releasing $MEDEA_CRATE_NAME to crates.io..."
      deploy:
        provider: script
        skip_cleanup: true
        script: make release.crates crate=$MEDEA_CRATE_NAME publish=yes
        on:
          tags: true

    - name: NPM
      stage: release
      if: (tag IS present) AND (tag =~ ^medea-jason-[0-9]+\.[0-9]+\.[0-9]+)
      cache: false
      before_script:
        - export MEDEA_CRATE_NAME=$(echo $TRAVIS_TAG \
                        | sed -E "s/^(medea[a-z-]*)-[0-9]+\.[0-9]+\.[0-9]+/\1/")
      script:
        - echo "Releasing $MEDEA_CRATE_NAME to NPM..."
      before_deploy:
        - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
        - curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      deploy:
        provider: script
        skip_cleanup: true
        script: make release.npm crate=$MEDEA_CRATE_NAME publish=yes
        on:
          tags: true

    - name: GitHub
      stage: release
      if: (tag IS present) AND (tag =~ ^medea[a-z-]*-[0-9]+\.[0-9]+\.[0-9]+)
      cache: false
      before_script:
        - export MEDEA_CRATE_NAME=$(echo $TRAVIS_TAG \
                        | sed -E "s/^(medea[a-z-]*)-[0-9]+\.[0-9]+\.[0-9]+/\1/")
      script:
        - echo "Releasing $MEDEA_CRATE_NAME to GitHub..."
      deploy:
        provider: releases
        skip_cleanup: true
        api_key: $GH_TOKEN
        name: $TRAVIS_TAG
        on:
          tags: true

stages:
  - name: check
    if: (branch = master AND type != pull_request) OR commit_message =~ /.*\[run ci\]/
  - name: build
    if: (branch = master AND type != pull_request) OR commit_message =~ /.*\[run ci\]/
  - name: test
    if: (branch = master AND type != pull_request) OR commit_message =~ /.*\[run ci\]/
  - name: tests
    if: (branch = master AND type != pull_request) OR commit_message =~ /.*\[run ci\]/

notifications:
  email:
    on_success: never
    on_failure: always
